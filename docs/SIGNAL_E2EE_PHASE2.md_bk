# Signal-style E2EE (Phase 2) — Implementation Plan

Status: **IN PROGRESS**
Branch: `feature/signal-e2ee-phase2`

## Goal
Implement end-to-end encryption for app↔bridge communications inspired by Signal architecture (X3DH + Double Ratchet semantics), while keeping compatibility with media attachments and current chat UX.

## Security target
- Bridge can route ciphertext but cannot read plaintext message content after full migration mode.
- Session keys rotate forward-securely.
- Replay protection with message counters and associated data (AD).

## Protocol shape (target)
1. **Identity + prekeys**
   - Device identity key pair (long-term)
   - Signed prekey (medium-term)
   - One-time prekeys (batch)
2. **Session bootstrap**
   - Client fetches bridge prekey bundle
   - Client performs X3DH-style agreement
   - First encrypted envelope includes initial ratchet header
3. **Transport envelope**
   - `v`: protocol version
   - `sessionId`: logical app session
   - `counter`: monotonic per ratchet chain
   - `nonce` / `header`
   - `ciphertext`
   - `ad`: optional associated data (timestamp, message id)
4. **Attachments**
   - Encrypt attachment payload before base64 transport
   - Include MIME/name only as needed metadata (minimal leakage)
5. **Recovery**
   - Session reset endpoint
   - Safe fallback (explicitly disabled when strict-E2EE enabled)

## Migration stages
### Stage A (started in this branch)
- Add E2EE capability flags and strict enforcement mode in bridge.
- Add E2EE status endpoint for client capability negotiation.

### Stage B (bootstrap implemented in this branch)
- [x] Add initial key storage helper on Android client.
- [x] Add bridge prekey bundle endpoint (`/e2ee/prekey-bundle`) for capability bootstrap.
- [ ] Replace development key material with real X25519/Ed25519 primitives and signed rotation.

### Stage C (implemented in this branch)
- [x] Add encrypted `/chat` envelope path and bridge-side decryption pipeline.
- [x] Add encrypted response envelope path (`e2eeReply`) and client-side decryption.
- [x] Replace dev stream cipher with real ECDH P-256 + HKDF-SHA256 + AES-256-GCM.
- [ ] Migrate to full Signal Double Ratchet lifecycle (Stage E-hardening).

### Stage D
- Add attachment encryption and transcript/TTS path compatibility.

### Phase 3 progress
- [x] Monotonic counter in encrypted envelopes.
- [x] Bridge replay/reorder rejection using persistent ratchet counter state.
- [x] One-time prekey consumption integrated with request envelopes.

### Phase 4 progress
- [x] Attachment encryption in client using E2EE session key material.
- [x] Bridge-side attachment decryption before processing (STT/image/video flow preserved).

### Phase 5 progress
- [x] Directional per-message key derivation (`c2s`, `s2c`, `att`) from base session key + counter.
- [x] Response decryption tied to response counter and direction label.
- [x] Ratchet baseline prepared for chain/message-key evolution.

### Phase 6 progress
- [x] Skipped/reorder window handling (client + bridge) with replay guard.
- [x] Initial DH ratchet step mixing via `ratchetPub` contribution in key schedule.
- [x] Persistent ratchet metadata per session at bridge side.

### Block close checkpoint (implemented)
- [x] Split send/recv chain state at bridge ratchet store (with legacy migration).
- [x] Persistent skipped-counter window on bridge (`recv.skippedIn`) and client (`e2ee_in_skipped_<session>`).
- [x] Directional chain-key derivation (`send`/`recv`) used consistently for `c2s`, `s2c`, and `att`.
- [x] Session base-key persistence on client for encrypted reply recovery after lifecycle changes.

### Next block
- [x] Header-based skipped-key cache (`headerId + counter`) with header-scoped retrieval/consumption.
- [ ] Full Double Ratchet state machine (`rootKey`, `sendChainKey`, `recvChainKey`, counters) with DH-step ratcheting.

### Stage E
- Remove plaintext path in strict deployments.

## Crypto stack (proposed)
- Curves/key agreement: X25519
- KDF: HKDF-SHA256
- AEAD: AES-256-GCM (or ChaCha20-Poly1305 if consistently available)
- Signature for prekeys: Ed25519

## Operational notes
- Keep TLS enabled even with E2EE (defense in depth)
- Key rotation and backup policy must be documented before production rollout
- Multi-device support requires per-device key identity mapping

## Immediate next coding tasks
1. Add bridge prekey-bundle endpoint with signed metadata.
2. Implement Android key manager (identity + signed prekey cache).
3. Add encrypted envelope parser/serializer in app.
4. Introduce feature flag in app settings: `Require E2EE`.
5. Add integration test vectors for envelope encryption/decryption.

## Watchdog governance checkpoint (2026-03-26 09:14 UTC)

### Estat real del progrés
- `aigor-app`: release gate PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `full_matrix` PASS + `strict_mode` PASS però amb superfície curta (`4` casos) + `assembleDebug PASS`.
- El progrés funcional està alt, però **DONE no assolit** per manca de paritat de cobertura strict entre repos.
- Aquesta execució no redueix gaps crítics: revalida estat a 09:14 UTC i confirma estancament de paritat (sense expansió de scope).

### Gaps de cobertura
1. Paritat strict (`malformed_input`, `counter_enforcement`) no sincronitzada: `aigor=100` vs `openclaw=4`.
2. Invariants avançats (window eviction, failed decrypt rollback, dh ratchet lifecycle) només validats a `aigor-app`.
3. Categoria `bidirectional_sync` en estat FAIL fins tenir matriu equivalent a tots dos repos.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync` per paritat)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **NO sincronitzat completament**.
- Codi i validació avançada existeixen principalment a `aigor-app`; `openclaw-app` manté subset reduït.

### Decisió watchdog
- No expandir scope amb nous REQUIRED ni pujar llindars.
- Prioritat única següent: tancar gap de paritat crítica entre repos sobre invariants existents.

## Watchdog governance checkpoint (2026-03-26 09:18 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- Progrés funcional mantingut, però **DONE no assolit** per gap de paritat strict entre repos.
- Aquesta execució és de validació: no s'ha reduït cap gap crític.

### Gaps de cobertura
1. `counter_enforcement` no té paritat (`aigor` cobert en profunditat, `openclaw` cobertura mínima de 4 casos strict).
2. `malformed_input` amb cobertura extensa només a `aigor-app`.
3. `bidirectional_sync` continua en FAIL fins sincronitzar la superfície strict i invariants crítics.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcialment sincronitzat**: branch igual (`feature/signal-e2ee-phase2`) i builds en verd als dos repos.
- **No sincronitzat en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- No crear nous REQUIRED/REGRESSION.
- No incrementar `minimStrictCases`.
- Següent acció vàlida: portar pack mínim de paritat strict a `openclaw-app` (classificat com CANDIDATE fins validar no-redundància).

## Watchdog governance checkpoint (2026-03-26 09:22 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: persisteix gap de paritat strict entre repos (`100` vs `4`).
- Execució de validació; no es resol cap FAIL REQUIRED/REGRESSION.

### Gaps de cobertura
1. `counter_enforcement` sense paritat completa a `openclaw-app`.
2. `malformed_input` sense superfície equivalent a `openclaw-app`.
3. `bidirectional_sync` continua FAIL mentre no hi hagi paritat d’invariants crítics.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca (`feature/signal-e2ee-phase2`) i build/checks en verd.
- **No equivalent en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: reduir gap crític de paritat strict a `openclaw-app` sobre invariants ja definits.

## Watchdog governance checkpoint (2026-03-26 09:26 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: continua la divergència de paritat strict (`aigor=100` vs `openclaw=4`).
- Execució de revalidació; no es resol cap FAIL REQUIRED ni REGRESSION.

### Gaps de cobertura
1. `counter_enforcement` sense paritat funcional de cobertura strict a `openclaw-app`.
2. `malformed_input` sense superfície equivalent a `openclaw-app`.
3. `bidirectional_sync` continua en FAIL fins sincronitzar invariants crítics entre repos.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca (`feature/signal-e2ee-phase2`) i comprovacions/builds en verd.
- **No equivalent en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- Sense canvis a `minimStrictCases`.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: reduir gap crític de paritat strict a `openclaw-app` sobre invariants existents.

## Watchdog governance checkpoint (2026-03-26 09:30 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: persisteix la divergència de cobertura strict (`aigor=100` vs `openclaw=4`).
- Execució de revalidació: no s’ha resolt cap FAIL de REQUIRED/REGRESSION.

### Gaps de cobertura
1. `counter_enforcement` continua sense paritat strict a `openclaw-app`.
2. `malformed_input` continua sense superfície equivalent a `openclaw-app`.
3. `bidirectional_sync` es manté en FAIL mentre no hi hagi paritat real d’invariants crítics.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca (`feature/signal-e2ee-phase2`) i checks/build en verd als dos repos.
- **No equivalent en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- Sense canvis a `minimStrictCases`.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: reduir gap crític de paritat strict a `openclaw-app` sobre invariants existents.

## Watchdog governance checkpoint (2026-03-26 09:34 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: persisteix la divergència de cobertura strict (`aigor=100` vs `openclaw=4`).
- Execució de revalidació: no s’ha resolt cap FAIL de REQUIRED/REGRESSION.

### Gaps de cobertura
1. `counter_enforcement` continua sense paritat strict a `openclaw-app`.
2. `malformed_input` continua sense superfície equivalent a `openclaw-app`.
3. `bidirectional_sync` es manté en FAIL mentre no hi hagi paritat real d’invariants crítics.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca (`feature/signal-e2ee-phase2`) i checks/build en verd als dos repos.
- **No equivalent en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- Sense canvis a `minimStrictCases`.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: reduir gap crític de paritat strict a `openclaw-app` sobre invariants existents.

## Watchdog governance checkpoint (2026-03-26 09:38 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: es manté la divergència de cobertura strict (`aigor=100` vs `openclaw=4`).
- Execució de revalidació estricta: cap FAIL de REQUIRED/REGRESSION resolt en aquesta passada.

### Gaps de cobertura
1. `counter_enforcement` sense paritat strict a `openclaw-app`.
2. `malformed_input` amb cobertura extensa només a `aigor-app`.
3. `bidirectional_sync` continua en FAIL mentre la paritat d’invariants crítics no sigui equivalent.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca (`feature/signal-e2ee-phase2`) i builds/checks en verd als dos repos.
- **No equivalent en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- `minimStrictCases` congelat i sense canvis.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: executar només feina de paritat strict a `openclaw-app` sobre invariants ja definits.

## Watchdog governance checkpoint (2026-03-26 10:02 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: la divergència strict persisteix (`aigor=100` vs `openclaw=4`).
- Execució de control: sense resolució de FAIL REQUIRED/REGRESSION en aquesta passada.

### Gaps de cobertura
1. `counter_enforcement` sense paritat strict a `openclaw-app`.
2. `malformed_input` amb cobertura extensa només a `aigor-app`.
3. `bidirectional_sync` continua en FAIL mentre no hi hagi equivalència d’invariants crítics entre repos.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca `feature/signal-e2ee-phase2` i checks/build en verd als dos repos.
- **No sincronitzat en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- `minimStrictCases` congelat i sense canvis.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: tancar paritat strict mínima d’invariants existents a `openclaw-app`.

## Watchdog governance checkpoint (2026-03-26 10:06 UTC)

### Estat real del progrés
- `aigor-app`: `python3 scripts/e2ee_release_gate_smoke.py` PASS (`fullMatrix=19/19`, `strictCases=100`, `assembleRelease PASS`).
- `openclaw-app`: `python3 scripts/e2ee_full_matrix_smoke.py` PASS + `python3 scripts/e2ee_strict_mode_smoke.py scripts/openclaw_chat_bridge.py OPENCLAW_APP` PASS (`4` casos) + `./gradlew :app:assembleDebug` PASS.
- **DONE no assolit**: persisteix divergència strict (`aigor=100` vs `openclaw=4`).
- Execució de control: cap FAIL REQUIRED/REGRESSION resolt en aquesta passada.

### Gaps de cobertura
1. `counter_enforcement` continua sense paritat strict a `openclaw-app`.
2. `malformed_input` continua sense superfície equivalent a `openclaw-app`.
3. `bidirectional_sync` es manté en FAIL mentre no hi hagi paritat real d’invariants crítics.

### Estat REQUIRED i REGRESSION (font de veritat: `docs/e2ee_strict_coverage_matrix.md`)
- REQUIRED PASS: 11
- REQUIRED FAIL: 3 (`counter_enforcement`, `malformed_input`, `bidirectional_sync`)
- REGRESSION PASS: 3
- REGRESSION FAIL: 0

### Estat de sincronització entre repos
- **Parcial**: mateixa branca `feature/signal-e2ee-phase2` i checks/build en verd als dos repos.
- **No sincronitzat en cobertura strict**: `aigor strictCases=100` vs `openclaw strictCases=4`.

### Decisió watchdog
- Sense expansió de scope.
- `minimStrictCases` congelat i sense canvis.
- Sense nous REQUIRED/REGRESSION/CANDIDATE.
- Proper pas únic vàlid: tancar paritat strict mínima d’invariants existents a `openclaw-app`.

WRITE_20269326
